export const useStyles = theme => ({
    divider: {
        margin: theme.spacing(2, 0),
    },
    formControl: {
        margin: theme.spacing(1),
        minWidth: 120,
    },
    boxButtons: {
        "margin-top": 30,
    },
    footer: {
        padding: theme.spacing(3, 2),
        marginTop: 'auto',
    },
});